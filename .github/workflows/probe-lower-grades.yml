name: Probe live rugby league sources
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/probe_championdata.py'
      - 'scripts/probe_nrl_official.py'
      - '.github/workflows/probe-lower-grades.yml'
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Test official 2026 NRL refresh
        run: |
          python scripts/refresh_nrl_2026.py
          python scripts/check_2026_freshness.py
