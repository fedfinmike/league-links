function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function setupAutocomplete(input,box,onPick){let selected=-1;input.addEventListener('input',()=>{selected=-1;render()});input.addEventListener('focus',render);input.addEventListener('keydown',e=>{const items=[...box.querySelectorAll('.suggestion')];if(!items.length)return;if(e.key==='ArrowDown'){e.preventDefault();selected=Math.min(items.length-1,selected+1)}else if(e.key==='ArrowUp'){e.preventDefault();selected=Math.max(0,selected-1)}else if(e.key==='Enter'&&selected>=0){e.preventDefault();items[selected].click();return}else if(e.key==='Escape'){box.classList.remove('show');return}items.forEach((x,i)=>x.classList.toggle('selected',i===selected))});function render(){const rs=findPlayers(input.value);box.innerHTML='';if(!rs.length){box.classList.remove('show');return}for(const p of rs){const m=playerMeta(p.id),b=document.createElement('button');b.className='suggestion';b.innerHTML=`<span>${esc(m.name)}</span><small>${esc(m.latestTeam)}${m.span?` · ${m.span.first}–${m.span.last}`:''}</small>`;b.onclick=()=>{input.value=m.name;box.classList.remove('show');onPick(String(m.id))};box.appendChild(b)}box.classList.add('show')}document.addEventListener('click',e=>{if(e.target!==input&&!box.contains(e.target))box.classList.remove('show')})}
function route(name,params={},push=true){let h=name;if(name==='player'&&params.id)h=`player/${params.id}`;if(name==='connect'&&params.a&&params.b)h=`connect/${params.a}/${params.b}`;if(name==='teams'&&params.competition&&params.year&&params.team)h=`teams/${encodeURIComponent(params.competition)}/${params.year}/${encodeURIComponent(params.team)}`;if(push)location.hash=h;else applyRoute(h)}
function currentRoute(){return(location.hash||'#home').slice(1)}
function applyRoute(raw=currentRoute()){const bits=raw.split('/'),name=bits[0]||'home',viewName=name==='player'?'players':name;$$('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${viewName}`));$$('[data-route]').forEach(b=>b.classList.toggle('active',b.dataset.route===viewName||(viewName==='players'&&b.dataset.route==='players')));$('#pageTitle').textContent={home:'Rugby league connections',players:'Players',player:'Player',connect:'Connect players',teams:'Teams',competitions:'Competitions'}[name]||'League Links';if(name==='player'&&bits[1])openPlayer(bits[1],false);if(name==='connect'&&bits[1]&&bits[2]){state.connectA=bits[1];state.connectB=bits[2];$('#connectA').value=playerMeta(bits[1]).name;$('#connectB').value=playerMeta(bits[2]).name;renderConnection()}if(name==='teams'&&bits[1]){const c=decodeURIComponent(bits[1]),y=bits[2],t=decodeURIComponent(bits.slice(3).join('/'));$('#teamCompetition').value=c;fillTeamYears(c,y);fillTeams(c,y,t);renderRoster()}window.scrollTo({top:0,behavior:'smooth'})}
window.addEventListener('hashchange',()=>applyRoute())
function recentIds(){try{return JSON.parse(localStorage.getItem('ll-recent')||'[]')}catch{return[]}}function favIds(){try{return JSON.parse(localStorage.getItem('ll-favs')||'[]')}catch{return[]}}function remember(id){let a=recentIds().filter(x=>x!==String(id));a.unshift(String(id));localStorage.setItem('ll-recent',JSON.stringify(a.slice(0,10)));renderHomePeople()}function toggleFav(id){let a=favIds(),s=String(id);a=a.includes(s)?a.filter(x=>x!==s):[s,...a];localStorage.setItem('ll-favs',JSON.stringify(a));renderHomePeople();if(state.selectedPlayer===s)renderPlayer(s);toast(a.includes(s)?'Saved':'Removed')}
function personCard(id){const m=playerMeta(id);return`<button class="person" data-player="${esc(id)}"><span class="star ${favIds().includes(String(id))?'on':''}">★</span><span class="initials">${initials(m.name)}</span><strong>${esc(m.name)}</strong><small>${esc(m.latestTeam)}</small></button>`}
function renderHomePeople(){const r=recentIds().filter(id=>state.appearances.has(String(id))).slice(0,5),f=favIds().filter(id=>state.appearances.has(String(id))).slice(0,5);$('#recentPeople').innerHTML=r.map(personCard).join('')||`<div class="card empty" style="grid-column:1/-1"><strong>No recent players yet.</strong>Open a player and they will appear here.</div>`;$('#favPeople').innerHTML=f.map(personCard).join('')||`<div class="card empty" style="grid-column:1/-1"><strong>No favourites yet.</strong>Use the star on a player page to save one.</div>`;$$('[data-player]').forEach(b=>b.onclick=()=>route('player',{id:b.dataset.player}))}
$('#clearRecent').onclick=()=>{localStorage.removeItem('ll-recent');renderHomePeople()}
function histories(a,b){const hm=state.historyByPair.get(pairKey(String(a),String(b)));return hm?[...hm.values()].sort((x,y)=>x.year-y.year||x.competition.localeCompare(y.competition)):[]}
function filteredEdgeGames(a,b,from,to,competition){return histories(a,b).filter(h=>(!from||h.year>=from)&&(!to||h.year<=to)&&(!competition||competition==='All'||h.competition===competition)).reduce((s,h)=>s+h.games,0)}
function openPlayer(id,push=true){id=String(id);if(!state.appearances.has(id))return;state.selectedPlayer=id;state.ledgerLimit=50;remember(id);if(push){route('player',{id});return}renderPlayer(id)}
function renderPlayer(id){id=String(id);const m=playerMeta(id),adj=state.adj.get(id)||[],fav=favIds().includes(id);$('#ledgerHeading').textContent=m.name;$('#ledgerIntro').textContent='Match-confirmed history includes NRL, State of Origin and International matches from 2000 where exact line-ups are available, plus the state-cup data currently loaded.';$('#playerContent').innerHTML=`<div class="card playerhero"><div class="playerrow"><div class="playerid"><div class="avatar">${initials(m.name)}</div><div><h2>${esc(m.name)}</h2><p>${esc(m.latestTeam)}${m.span?` · ${m.span.first}–${m.span.last}`:''}</p></div></div><div class="playeractions"><button class="ghost" id="copyPlayer">Copy link</button><button class="iconbtn" id="favPlayer" title="Favourite">${fav?'★':'☆'}</button></div></div><div class="metrics"><div class="metric"><strong>${fmt(m.appearances)}</strong><span>Games recorded</span></div><div class="metric"><strong>${fmt(m.teammates)}</strong><span>Teammates</span></div><div class="metric"><strong>${m.span?m.span.last-m.span.first+1:0}</strong><span>Years covered</span></div><div class="metric"><strong>${fmt(m.competitions)}</strong><span>Competitions</span></div></div></div><div class="subtabs"><button class="subtab ${state.ledgerTab==='ledger'?'active':''}" data-subtab="ledger">Teammates</button><button class="subtab ${state.ledgerTab==='career'?'active':''}" data-subtab="career">Career</button></div><div id="playerSub"></div><aside class="ad-slot ad-inline" aria-label="Advertisement"><span>Advertisement</span><div><strong>Partner space</strong><small>A small ad here helps keep League Links free.</small></div></aside>`;$('#copyPlayer').onclick=()=>navigator.clipboard.writeText(location.href).then(()=>toast('Link copied'));$('#favPlayer').onclick=()=>toggleFav(id);$$('[data-subtab]').forEach(b=>b.onclick=()=>{state.ledgerTab=b.dataset.subtab;renderPlayer(id)});if(state.ledgerTab==='career')renderCareer(id);else renderLedger(id,adj)}
function renderLedger(id,adj){const m=playerMeta(id),start=m.span?.first||2000,end=m.span?.last||2026,compOptions=['All',...availableCompetitionIds()];$('#playerSub').innerHTML=`<div class="toolbar"><div class="field"><label>Find a teammate</label><input id="mateFilter" placeholder="Type a name…"></div><div class="field"><label>Competition</label><select id="mateComp">${compOptions.map(x=>`<option>${esc(x)}</option>`).join('')}</select></div><div class="field"><label>From</label><select id="mateFrom">${yearOptions(start,end,start)}</select></div><div class="field"><label>To</label><select id="mateTo">${yearOptions(start,end,end)}</select></div><button class="ghost" id="resetLedger">Reset</button></div><div id="ledgerTable"></div>`;const redraw=()=>{const q=$('#mateFilter').value.toLowerCase(),from=Number($('#mateFrom').value)||0,to=Number($('#mateTo').value)||9999,comp=$('#mateComp').value;let rows=adj.map(e=>({...e,name:playerMeta(e.id).name,g:filteredEdgeGames(id,e.id,from,to,comp)})).filter(e=>e.g>0&&e.name.toLowerCase().includes(q)).sort((a,b)=>b.g-a.g||a.name.localeCompare(b.name));const shown=rows.slice(0,state.ledgerLimit);$('#ledgerTable').innerHTML=`<div class="tablewrap"><table><thead><tr><th>#</th><th>Teammate</th><th>Games together</th><th>Years</th><th>Where</th><th></th></tr></thead><tbody>${shown.map((e,i)=>{const hs=histories(id,e.id).filter(h=>h.year>=from&&h.year<=to&&(comp==='All'||h.competition===comp)),ys=[...new Set(hs.map(h=>h.year))],cs=[...new Set(hs.map(h=>competitionShort(h.competition)))];return`<tr><td>${i+1}</td><td><button class="playerlink" data-open="${e.id}">${esc(e.name)}</button></td><td class="games">${e.g}</td><td class="yearspan">${ys.length?`${Math.min(...ys)}–${Math.max(...ys)}`:'—'}</td><td class="yearspan">${esc(cs.join(', ')||'—')}</td><td><button class="historybtn" data-history="${e.id}">Shared history</button></td></tr>`}).join('')}</tbody></table></div>${rows.length>shown.length?`<div class="loadmore"><button class="ghost" id="moreLedger">Show more · ${rows.length-shown.length} left</button></div>`:''}`;$$('[data-open]').forEach(b=>b.onclick=()=>openPlayer(b.dataset.open));$$('[data-history]').forEach(b=>b.onclick=()=>openHistory(id,b.dataset.history));if($('#moreLedger'))$('#moreLedger').onclick=()=>{state.ledgerLimit+=50;redraw()}};$('#mateFilter').oninput=redraw;$('#mateComp').onchange=redraw;$('#mateFrom').onchange=redraw;$('#mateTo').onchange=redraw;$('#resetLedger').onclick=()=>{state.ledgerLimit=50;$('#mateFilter').value='';$('#mateComp').value='All';$('#mateFrom').value=start;$('#mateTo').value=end;redraw()};redraw()}
function yearOptions(start,end,selected){let out='';for(let y=start;y<=end;y++)out+=`<option value="${y}" ${y===selected?'selected':''}>${y}</option>`;return out}
function renderCareer(id){const ym=state.career.get(id);if(!ym){$('#playerSub').innerHTML='<div class="card empty">No games loaded yet.</div>';return}const rows=[];for(const[year,tm]of[...ym.entries()].sort((a,b)=>b[0]-a[0]))for(const[key,games]of[...tm.entries()].sort((a,b)=>b[1]-a[1])){const{competition,team}=splitCareerKey(key);rows.push({year,competition,team,games})}$('#playerSub').innerHTML=`<div class="timeline">${rows.map(r=>`<div class="seasonrow"><div class="year">${r.year}</div><div class="club">${esc(r.team)}<small>${esc(r.competition)}</small></div><div class="count">${r.games} game${r.games===1?'':'s'}</div></div>`).join('')}</div>`}
function openHistory(a,b){const A=playerMeta(a),B=playerMeta(b),hs=histories(a,b);if(!hs.length)return;const total=hs.reduce((s,h)=>s+h.games,0),years=[...new Set(hs.map(h=>h.year))],max=Math.max(...hs.map(h=>h.games));$('#drawer').innerHTML=`<div class="drawerhead"><div><p class="eyebrow">SHARED HISTORY</p><h2>${esc(A.name)} × ${esc(B.name)}</h2><p>Games where both players were recorded for the same team.</p></div><button class="iconbtn" id="closeDrawer">×</button></div><div class="sharedstats"><div class="sharedstat"><strong>${total}</strong><span>Games together</span></div><div class="sharedstat"><strong>${years.length}</strong><span>Shared years</span></div><div class="sharedstat"><strong>${Math.min(...years)}–${Math.max(...years)}</strong><span>Shared span</span></div></div><div class="bars">${[...hs].sort((x,y)=>y.year-x.year||x.competition.localeCompare(y.competition)).map(h=>`<div class="barrow"><div class="yr">${h.year}</div><div class="bar"><i style="width:${Math.max(6,h.games/max*100)}%"></i></div><div class="n">${h.games}</div><div class="barclub">${esc(h.team)} · ${esc(competitionShort(h.competition))}</div></div>`).join('')}</div><div class="heroactions" style="margin-top:22px"><button class="ghost" data-drawer-player="${a}">Open ${esc(A.name)}</button><button class="primary" data-drawer-player="${b}">Open ${esc(B.name)}</button></div>`;$('#drawerBack').classList.add('show');$('#closeDrawer').onclick=closeDrawer;$$('[data-drawer-player]').forEach(x=>x.onclick=()=>{closeDrawer();openPlayer(x.dataset.drawerPlayer)})}
function closeDrawer(){$('#drawerBack').classList.remove('show')}$('#drawerBack').onclick=e=>{if(e.target===$('#drawerBack'))closeDrawer()};document.addEventListener('keydown',e=>{if(e.key==='Escape')closeDrawer()})
